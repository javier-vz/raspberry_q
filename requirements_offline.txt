# requirements_offline.txt
# Qoyllur Rit'i Explorer — versión offline para Raspberry Pi 5
# Sin dependencias de red (sin groq, sin openai)

streamlit>=1.32.0
rdflib>=7.0.0
sentence-transformers>=2.2.0
numpy>=1.24.0
rank-bm25>=0.2.2
python-dotenv>=1.0.0
reportlab>=4.0.0
scikit-learn>=1.0.0

# PyTorch CPU (para sentence-transformers en ARM)
# Instalar con: pip install torch --index-url https://download.pytorch.org/whl/cpu
# O simplemente: pip install torch  (detecta arquitectura automáticamente)
